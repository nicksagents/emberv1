# AGENTS

This project uses React with Vite.

- Keep the app lightweight before introducing routing or global state libraries.
- Use `src/App.tsx` as the first implementation surface unless a stronger structure emerges.
- Preserve the scaffold's visual direction until the user asks for a new one.
