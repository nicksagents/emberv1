---
name: project-stack
description: Local conventions for the generated React + Vite scaffold.
roles: [coordinator, director, inspector]
---

## React + Vite Project Stack

- Prefer simple component files under `src/` before adding extra architecture.
- Keep TypeScript strict and avoid unnecessary abstraction on the first feature pass.
- Replace placeholder cards with the requested UI as soon as implementation begins.
