# AGENTS

This project is a plain Node.js HTTP service.

- Stay on built-in Node APIs unless the feature set clearly needs a framework.
- Keep routes small and explicit.
- Add tests as soon as real behavior replaces the placeholder route.
