# AGENTS

This project is a Rust CLI starter.

- Keep the dependency surface small.
- Prefer explicit structs and enums over stringly-typed flows.
- Split into modules only when the first feature genuinely needs it.
