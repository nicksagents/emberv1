---
name: project-stack
description: Local conventions for the generated Next.js app scaffold.
roles: [coordinator, director, inspector]
---

## Next.js Project Stack

- This project uses the App Router under `src/app/`.
- Preserve the current visual language unless the user explicitly wants a redesign.
- Add new routes and route groups under `src/app/` instead of inventing a parallel structure.
- Keep placeholder scaffold text only until the first real feature slice is implemented.
