# AGENTS

This project is a TypeScript library starter.

- Keep the public API centered in `src/index.ts` until the package earns more modules.
- Preserve strict typing.
- Add tests for every exported behavior change.
