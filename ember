#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOT_DIR"

COMMAND="${1:-start}"

if [ -f "packages/cli/dist/index.js" ]; then
  node packages/cli/dist/index.js "$@"
else
  COREPACK_HOME="${COREPACK_HOME:-/tmp/corepack}" pnpm --filter @ember/cli dev "$@"
fi
