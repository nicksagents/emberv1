{
  "name": "@ember/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build",
    "dev": "next dev --hostname 0.0.0.0",
    "start": "next start --hostname 0.0.0.0",
    "test": "bash -lc 'shopt -s globstar nullglob; tests=(__tests__/**/*.test.ts __tests__/**/*.test.tsx); if [ ${#tests[@]} -eq 0 ]; then echo \"No test files found.\"; exit 0; fi; node --import tsx --test --test-concurrency=1 --test-force-exit \"${tests[@]}\"'",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ember/core": "workspace:*",
    "@ember/ui-schema": "workspace:*",
    "next": "^15.2.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
