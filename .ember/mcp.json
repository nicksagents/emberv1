{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "roles": ["coordinator", "advisor", "director", "inspector", "ops"],
      "timeout": 15000
    }
  }
}
